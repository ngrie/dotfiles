export PATH="$HOME/.bin:$HOME/.composer/vendor/bin:$PATH"

export EDITOR="vim"

# Volta.sh integration (install by running "curl https://get.volta.sh | bash -s -- --skip-setup")
export VOLTA_HOME="$HOME/.volta"
export PATH="$VOLTA_HOME/bin:$PATH"
